import { Button } from "@nextui-org/react";

function BookACall() {
  return (
    <Button color="gradient" auto css={{ mr: "$xs" }}>
      Book a Call
    </Button>
  );
}

export default BookACall;
